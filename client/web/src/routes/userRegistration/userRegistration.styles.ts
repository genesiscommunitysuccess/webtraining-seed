import {css} from '@microsoft/fast-element';

export const userRegistrationStyles = css`

    #newUserForm {
        display: flex;
        flex-direction: column;
        width: 300px;
    }

    zero-modal {
        overflow-y: scroll;
        overflow-x: hidden;
    }


`;