import {css} from '@microsoft/fast-element';

export const playgroundStyles = css`

    #FormsModal {
        display: flex;
        flex-direction: column;
        width: 300px;
    }

    .ScrollOn {
        overflow-y: scroll;
        overflow-x: hidden;
    }

`;